name: CI Complète

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]
  workflow_dispatch:

jobs:
  call-lint:
    uses: ./.github/workflows/01-lint.yml

  call-snyk:
    uses: ./.github/workflows/02-snyk.yml
    secrets: inherit

  call-sonarqube:
    uses: ./.github/workflows/03-sonarqube.yml
    secrets: inherit
    permissions:
      contents: read
      pull-requests: read

  call-docker:
    needs: [call-lint, call-snyk, call-sonarqube]
    uses: ./.github/workflows/04-docker.yml

  call-docs:
    needs: [call-docker]
    uses: ./.github/workflows/05-docs.yml
    permissions:
      contents: write
      pull-requests: read

  notification:
    needs: call-docs
    uses: ./.github/workflows/06-completed.yml
